language: php
php: "5.3.3"
before_script:
  - sh -c "if [ '$TRAVIS_PHP_VERSION' \< '5.4' ]; then printf '\n' | pecl install xslcache-beta; fi"
script: "phpunit --exclude-group needs-network"
branches:
  only: dev/php5.3